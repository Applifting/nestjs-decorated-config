export enum MockSchemaEnum {
  OFF = 'off',
  MOCK_MISSING = 'mock_missing',
  MOCK_EVERYTHING = 'mock_everything',
}

export enum GraphqlIdeEnum {
  OFF = 'off',
  APOLLO_SANDBOX = 'apollo_sandbox',
  PLAYGROUND = 'playground',
}
